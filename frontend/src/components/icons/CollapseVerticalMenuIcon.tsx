import React from 'react';
import { Icon, IconProps } from './Icon';

export const CollapseVerticalMenuIcon: React.FC<IconProps> = (props) => (
  <Icon {...props}>
    <polyline points="18 15 12 9 6 15" />
  </Icon>
);

export default CollapseVerticalMenuIcon;
