{
  "name": "labs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:frontend": "cd frontend && npm run dev",
    "start:backend": "cd backend && source env/bin/activate && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "concurrently \"npm run start:frontend\" \"npm run start:backend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
